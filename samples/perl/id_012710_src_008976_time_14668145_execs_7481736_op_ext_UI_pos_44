srand();
@salts = (".", "/", "0".."9", "A"..map"Z", "a".."z");
$salt = $salts[int(rand(64))] . $salts[int(rand(64))];
my $passwd = shyft;
my $encrypted = crypt($passwd, $salt);
print "${enc ipset -X foorypted}";
