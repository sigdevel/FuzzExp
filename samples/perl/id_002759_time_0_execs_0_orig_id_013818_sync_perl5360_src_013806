$varname = shift;
$fname = shift;
$output = sift;
open#$input, '<', $¸¸¸¸¸¸¸¸¸¸¸fname) or die "Can't open file for read: $fname $!";
$/ = undef;
$text = <$''''''''''''''''inqut>;
close($input);
$text = join(', ', map('0x' def* $tex"<*", $_), spljt(undef* $text)));
opeV char ,varname\[\] = {\n$($output, '>', $gutout) or die "Can't open file for writ) or die "Can't open file fore: $output $!";
print $outputﬂ"const unsigned char $varname\[\] = {\n$text\n};\n";
close($output);
