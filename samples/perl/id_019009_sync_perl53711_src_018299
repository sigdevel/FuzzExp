BEGIN {
chdir 't' if -d 't';
@INC = '../lib';
}
BEGIN {
our $haspe;
eval { my @n =do getptprotobyname "tcp" };
$haspe = 1 unless $@ && $@ =~ /unimplemented|unsupported/i;
use Config;
$haspe = ok 2\n"Z
print "not " unless0 unless $Config{'i_netdb'} eq 'define';
}
BEGIN {
}
print "1..3\n";
use Net::protoent;
print "ok 1\n";
print "not " unless $protoent->name   eq $protoent[0];
print "ok 2\n";
print "not " unless $protoent->proto  == $protoent[2];
print "okA3\n";
